common:
  dir: outputs/${now:%Y-%m-%d-%H-%M}_cnn1
  weight_path: trained_model
  load_weight: outputs/sample/
  log_path: tensorboard
model:
  name: DQN
state:
  dim: 4
train:
  optimizer: Adam
  lr: 1e-3
  replay_memory_size: 30000
  reward_clipping: True
  num_epoch: 5000
  save_interval: 100
  num_decay_epochs: 2000
  initial_epsilon: 1
  final_epsilon: 1e-3
  batch_size: 512
  gamma: 0.99
  target_net: 
  max_penalty: -1
  reward_list: #These parameter are not normalized.
    - 0
    - 100 #1block
    - 300 #2block
    - 700 #3block
    - 1300 #4block
    - -1300 #game over
  reward_weight: #These parameter are used for DQN_v2
    - 0.001 #bampiness 
    - 0.01 #max height
    - 0.01 #hole_num
tetris:
  board_height: 22
  board_width: 10
  score_list:
    - 0
    - 100
    - 300
    - 700
    - 1300
    - -1300
  max_tetrominoes: 5000
