version: '1'

services:
  tetris:
    container_name: tetris-container
    build:
      context: .
      dockerfile: ./docker/Dockerfile.latest
    volumes:
      - type: bind
        source: "."
        target: "/tetris"
    tty: true
    shm_size: 512m