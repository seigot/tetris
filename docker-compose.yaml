version: '1'
  app:

services:
  tetris:
    container_name: tetris-container
    build:
      context: .
      dockerfile: ./docker/Dockerfile.latest
    environment:
      - FRAMEWORK=keras
    - DISPLAY=host.docker.internal:0.0
    tty: true
    ports:
    shm_size: 512m
      - "5000:5000"
